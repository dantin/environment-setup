---

extra_os_repository: http://dl.fedoraproject.org/pub/epel/epel-release-latest-7.noarch.rpm

packages:
  - curl              # curl utility
  - wget              # wget utility
  - unzip             # unzip utility
  - tar               # tar utility
  - tree              # tree utility
  - chrony            # ntp serive
  - yum-utils         # yum-config-manager utility

ntp_service_name: chronyd
