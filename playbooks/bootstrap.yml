---

- name: Preflight checks
  hosts: localhost
  gather_facts: false
  roles:
    - preflight-checks

- name: Prepare base system
  hosts: all
  gather_facts: false
  roles:
    - bootstrap
  become: true
